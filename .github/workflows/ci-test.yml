name: Legacy CI Tests

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  note:
    name: Replaced by CI
    runs-on: ubuntu-latest
    steps:
      - run: echo "Este workflow quedó manual. Usá .github/workflows/ci.yml como quality gate principal."
